name: Lint catalog

on:
  pull_request:
    paths:
      - "skills/**"
      - "src/**"
      - "package.json"

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm ci
      - run: npm run typecheck
      - run: npm test
      - name: Build CLI
        run: npm run build
      - name: Lint skills catalog
        run: node dist/index.js lint
      - name: Check index.json is up to date
        run: node dist/index.js index --check
